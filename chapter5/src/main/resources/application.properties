## PostgreSQL DataSource
spring.datasource.url=jdbc:postgresql://localhost:5432/chapter5?useSSL=false
spring.datasource.username=postgres
spring.datasource.password=081233

## JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# To format SQL queries generated by Hibernate
spring.jpa.properties.hibernate.format_sql=true

# change the port number
server.port=9191

# change Spring Boot logo
spring.banner.location=classpath:my-banner.txt

# Jackson timestamp
spring.jackson.time-zone=GMT+7